Code Block / Line,What is it?,Why are we doing this?
try (ByteArrayOutputStream baos = new ByteArrayOutputStream()) {,ByteArrayOutputStream (BAOS): A stream that writes its data into a byte array stored in memory.,"This is the ""bucket"" where the entire PDF file will be built in memory before being sent back to the user. We use try-with-resources to ensure it closes automatically."
PdfWriter writer = new PdfWriter(baos);,PdfWriter: The object that actually writes the raw PDF commands and data.,"The Writer is the ""pen"" that connects the PDF creation library (iText) to the memory bucket (the baos). Every command to add text or images goes through this."
PdfDocument pdf = new PdfDocument(writer);,PdfDocument: The internal representation of the PDF file format structure.,"This represents the entire digital PDF file structure itself, managing pages, fonts, and metadata. It uses the writer to output the final data."
Document document = new Document(pdf);,"Document: A high-level, easy-to-use container for adding visible content (text, tables, images).","This is the ""canvas"" you paint on. You use the Document object to add readable elements without worrying about the low-level PDF format details."
document.add(new Paragraph(...));,document.add(...): A simple command to place an element on the PDF canvas.,"We are adding blocks of text (Paragraph objects) to the PDF page, using the salary data we received from the database."
document.close();,document.close(): Finalizes the PDF structure.,"This is CRITICAL. It tells the Document to finish writing all pending content, close the pages, and instruct the PdfWriter to complete and seal the PDF file structure within the ByteArrayOutputStream."
return baos.toByteArray();,baos.toByteArray(): Gets the complete array of raw bytes.,"Once the PDF is sealed (close() was called), this extracts the final PDF file data from the memory bucket and returns it. This is what the Controller streams back to the user's browser."
catch (Exception e) { ... },Error Handling: Catches any error that might occur during the PDF creation process.,"If iText fails (e.g., if there's a problem with fonts or file access), we catch the error and throw a generic RuntimeException so the Spring Controller can return a clean 500 Internal Server Error to the React frontend."

That's a fundamental concept in computing! The byte and the byte[] (byte array) in the context of your PDF generation are the most basic way your computer handles any kind of data.Here's what a byte is, why it matters, and how it relates to your PDF.💾 What is a Byte?A byte is the fundamental unit of digital information storage.Definition: A single byte is a group of 8 bits (binary digits). A bit is the smallest unit of data and can only be a 0 or a 1.Capacity: Because a byte has 8 bits, it can represent $2^8 = 256$ different unique values (from 0 to 255).Examples:The letter 'A' is stored as a specific byte value (e.g., 01000001 in binary).A single red color component in an image is often stored as one byte.📑 The Byte Array (byte[])A byte array (byte[]) is simply a sequence, or list, of individual bytes stored back-to-back in memory.