spring.application.name=rest_be

spring.datasource.url=jdbc:mysql://172.31.7.53:3306/ESD
spring.datasource.username=root
spring.datasource.password=adiKar@29

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- JWT Configuration ---
# WARNING: This MUST be a long, random, and complex string (e.g., a Base64-encoded string)
# Keep it safe and don't hardcode sensitive info in a production environment.
jwt.secret-key=ZmFjdWx0eXNhbGFyeWhpc3Rvcnktc3VwZXItc2VjcmV0LWZvci1qc3Q=
jwt.expiration-ms=86400000
  # 24 hours in milliseconds
# --- SERVER BASE URL CONFIGURATION ---
# 1. Force the server port to be 8080 (if it's not already)
server.port=8080

# 2. Tell Spring Security to use the desired base URL when generating URIs for external services like Google.
# This prevents it from using dynamic IP addresses (like 172.x.x.x) in the OAuth callback.
#spring.security.oauth2.client.provider.google.issuer-uri=http://localhost:8080
# --- OAuth2 Configuration (Google Example) ---
spring.security.oauth2.client.registration.google.client-id=1098500003760-qh3ori7t3v1hl92o85o24imdjqb65i6o.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/oauth2/callback/google
spring.security.oauth2.client.registration.google.scope=openid,email,profile

# 3. Frontend Redirect URI (The URI where your React app captures the token)
app.oauth2.authorized-redirect-uri=http://localhost:5173/oauth2/redirect